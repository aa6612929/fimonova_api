services:
  - type: web
    name: fimonova-api
    env: python
    plan: free
    runtime: python3.11
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api_server_minimal:app --host 0.0.0.0 --port 10000"
